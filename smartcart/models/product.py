"""
Pydantic models for product data scraped from store search results,
plus the ProductSelection output from the LLM evaluation step.
"""
# TODO: Define Product (scraped), ProductQuery (parsed request), ProductSelection (LLM output)
